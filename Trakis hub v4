--==================== NOVA TELA DE CARREGAMENTO ====================

local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local gameName = MarketplaceService:GetProductInfo(game.PlaceId).Name

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TrakisLoading"
ScreenGui.IgnoreGuiInset = true
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui

-- FUNDO
local Background = Instance.new("Frame")
Background.Size = UDim2.fromScale(1, 1)
Background.BackgroundColor3 = Color3.fromRGB(3, 3, 3)
Background.BorderSizePixel = 0
Background.Parent = ScreenGui

local Gradient = Instance.new("UIGradient")
Gradient.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(100, 0, 0)),
	ColorSequenceKeypoint.new(0.45, Color3.fromRGB(25, 0, 0)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
}
Gradient.Rotation = 135
Gradient.Parent = Background

-- BRILHO VERMELHO
local Glow = Instance.new("Frame")
Glow.Size = UDim2.fromScale(0.7, 0.7)
Glow.Position = UDim2.fromScale(0.5, 0.43)
Glow.AnchorPoint = Vector2.new(0.5, 0.5)
Glow.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Glow.BackgroundTransparency = 0.92
Glow.BorderSizePixel = 0
Glow.Parent = Background

local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(1, 0)
GlowCorner.Parent = Glow

-- AVATAR
local Avatar = Instance.new("ImageLabel")
Avatar.Size = UDim2.fromOffset(115, 115)
Avatar.Position = UDim2.fromScale(0.5, 0.32)
Avatar.AnchorPoint = Vector2.new(0.5, 0.5)
Avatar.BackgroundTransparency = 1
Avatar.Image = Players:GetUserThumbnailAsync(
	player.UserId,
	Enum.ThumbnailType.HeadShot,
	Enum.ThumbnailSize.Size420x420
)
Avatar.Parent = Background

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0)
AvatarCorner.Parent = Avatar

local AvatarStroke = Instance.new("UIStroke")
AvatarStroke.Color = Color3.fromRGB(255, 0, 0)
AvatarStroke.Thickness = 3
AvatarStroke.Transparency = 0.15
AvatarStroke.Parent = Avatar

-- TÍTULO
local Title = Instance.new("TextLabel")
Title.Size = UDim2.fromScale(0.75, 0.09)
Title.Position = UDim2.fromScale(0.5, 0.49)
Title.AnchorPoint = Vector2.new(0.5, 0)
Title.BackgroundTransparency = 1
Title.Text = "TRAKIS HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBlack
Title.TextStrokeColor3 = Color3.fromRGB(255, 0, 0)
Title.TextStrokeTransparency = 0.35
Title.Parent = Background

-- SUBTÍTULO
local SubTitle = Instance.new("TextLabel")
SubTitle.Size = UDim2.fromScale(0.7, 0.045)
SubTitle.Position = UDim2.fromScale(0.5, 0.58)
SubTitle.AnchorPoint = Vector2.new(0.5, 0)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "Preparando o Hub..."
SubTitle.TextColor3 = Color3.fromRGB(255, 120, 120)
SubTitle.TextScaled = true
SubTitle.Font = Enum.Font.GothamMedium
SubTitle.Parent = Background

-- JOGO
local GameLabel = Instance.new("TextLabel")
GameLabel.Size = UDim2.fromScale(0.75, 0.045)
GameLabel.Position = UDim2.fromScale(0.5, 0.64)
GameLabel.AnchorPoint = Vector2.new(0.5, 0)
GameLabel.BackgroundTransparency = 1
GameLabel.Text = "Entrando em: " .. gameName
GameLabel.TextColor3 = Color3.fromRGB(190, 190, 190)
GameLabel.TextScaled = true
GameLabel.Font = Enum.Font.Gotham
GameLabel.Parent = Background

-- BARRA
local BarBG = Instance.new("Frame")
BarBG.Size = UDim2.fromScale(0.62, 0.018)
BarBG.Position = UDim2.fromScale(0.5, 0.76)
BarBG.AnchorPoint = Vector2.new(0.5, 0.5)
BarBG.BackgroundColor3 = Color3.fromRGB(45, 5, 5)
BarBG.BorderSizePixel = 0
BarBG.Parent = Background

local BarBGCorner = Instance.new("UICorner")
BarBGCorner.CornerRadius = UDim.new(1, 0)
BarBGCorner.Parent = BarBG

local Bar = Instance.new("Frame")
Bar.Size = UDim2.new(0, 0, 1, 0)
Bar.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Bar.BorderSizePixel = 0
Bar.Parent = BarBG

local BarCorner = Instance.new("UICorner")
BarCorner.CornerRadius = UDim.new(1, 0)
BarCorner.Parent = Bar

local BarGradient = Instance.new("UIGradient")
BarGradient.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 0, 0)),
	ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 40, 40)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
}
BarGradient.Parent = Bar

-- PORCENTAGEM
local Percent = Instance.new("TextLabel")
Percent.Size = UDim2.fromScale(0.25, 0.045)
Percent.Position = UDim2.fromScale(0.5, 0.80)
Percent.AnchorPoint = Vector2.new(0.5, 0)
Percent.BackgroundTransparency = 1
Percent.Text = "0%"
Percent.TextColor3 = Color3.fromRGB(255, 255, 255)
Percent.TextScaled = true
Percent.Font = Enum.Font.GothamBold
Percent.Parent = Background

-- LINHA DECORATIVA
local Line = Instance.new("Frame")
Line.Size = UDim2.fromScale(0.18, 0.003)
Line.Position = UDim2.fromScale(0.5, 0.70)
Line.AnchorPoint = Vector2.new(0.5, 0.5)
Line.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Line.BorderSizePixel = 0
Line.Parent = Background

-- ANIMAÇÃO DO BRILHO
task.spawn(function()
	while ScreenGui.Parent do
		TweenService:Create(
			Glow,
			TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
			{BackgroundTransparency = 0.96}
		):Play()

		task.wait(1.8)

		TweenService:Create(
			Glow,
			TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
			{BackgroundTransparency = 0.90}
		):Play()

		task.wait(1.8)
	end
end)

-- ANIMAÇÃO DO TÍTULO
task.spawn(function()
	while ScreenGui.Parent do
		TweenService:Create(
			Title,
			TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
			{TextStrokeTransparency = 0.05}
		):Play()

		task.wait(0.8)

		TweenService:Create(
			Title,
			TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
			{TextStrokeTransparency = 0.45}
		):Play()

		task.wait(0.8)
	end
end)

-- PARTÍCULAS LEVES
for i = 1, 18 do
	local Particle = Instance.new("Frame")

	Particle.Size = UDim2.fromOffset(
		math.random(2, 5),
		math.random(2, 5)
	)

	Particle.Position = UDim2.fromScale(
		math.random(),
		math.random()
	)

	Particle.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
	Particle.BackgroundTransparency = math.random(30, 70) / 100
	Particle.BorderSizePixel = 0
	Particle.Parent = Background

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(1, 0)
	Corner.Parent = Particle

	task.spawn(function()
		while ScreenGui.Parent do
			local newPosition = UDim2.fromScale(
				math.random(),
				math.random()
			)

			local tween = TweenService:Create(
				Particle,
				TweenInfo.new(
					math.random(3, 6),
					Enum.EasingStyle.Sine,
					Enum.EasingDirection.InOut
				),
				{
					Position = newPosition,
					BackgroundTransparency = math.random(40, 90) / 100
				}
			)

			tween:Play()
			tween.Completed:Wait()
		end
	end)
end

-- SOM
local Sound = Instance.new("Sound")
Sound.SoundId = "rbxassetid://94265985370740"
Sound.Volume = 0.7
Sound.Looped = false
Sound.Parent = Background
Sound:Play()

-- CARREGAMENTO
local totalTime = 20

for i = 1, 100 do
	Bar.Size = UDim2.new(i / 100, 0, 1, 0)
	Percent.Text = i .. "%"

	task.wait(totalTime / 100)
end

task.wait(0.5)

ScreenGui:Destroy()

task.wait(0.2)

-- Executa o Trakis Hub depois do loading
loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/trakishub/Trakis-Hub-v1/refs/heads/main/Trakis"
))()

--==================== FIM DA TELA ====================


